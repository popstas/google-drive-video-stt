# TODO

- [ ] add different openai processing presets. Example: transcript-cleanup -> keynotes + expertizeme-managers
- [ ] allow to define custom presets via config
- [ ] create data/config.yml, migrate .env to config.yml
- [ ] presets can depends on other presets. Example: all presets depends on transcript-cleanup, but keynotes and expertizeme-managers can be generated parallel.
